% intro_chip
echo on
%
% CREATE A VECTOR SET (FOR DEMO) AND DISPLAY
% ==========================================
%
% Let's create a demo vector set V to play with, which wie display.
% We use a vector set which displays a chip
%
;; V = vchip;     % V is a vector set
;; cls            % good practice to clear the screen
;; vplt(V,'k');   % plot the vector set
%
% The graphics looks nicer when we set 1:1 aspect ratio
%
;; set(gca,'DataAspectRatioMode','manual')
%
[];