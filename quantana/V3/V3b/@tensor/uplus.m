function out = uplus(obj)
% 
% UPLUS   Unary plus operation for tensors
%
%             S = space(tensor,1:3);
%             A = eye(S);                % operator on S
%
%         Operator sum
%
%            T = +S;
%            B = +A;
%
%
   out = obj;
   return
end

